<%= form_for(current_user.likes.find_by(tweet_id: like.tweet_id), html: { method: :delete }) do |f| %>
  <%= f.hidden_field :tweet_id, value: like.tweet_id %>
  <%= f.submit "Delete", class: 'btn btn-success' %>
<% end %>